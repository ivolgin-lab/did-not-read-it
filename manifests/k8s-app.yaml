apiVersion: app.k8s.io/v1beta1
kind: Application
metadata:
  name: didnotreadit
  labels:
    app.kubernetes.io/name: didnotreadit
spec:
  descriptor:
    links:
      - description: Open did not read it
        url: http://didnotreadit-app
